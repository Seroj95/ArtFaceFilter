<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ser.artface">

    <application
        android:name=".utilites.AppConfig"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtFace">

        <activity
            android:name=".activitys.savedImages.SavedImageActivity"
            android:exported="true" />
        <activity
            android:name=".activitys.filteredImage.FilteredImageActivity"
            android:exported="true" />
        <activity
            android:name=".activitys.editimage.GDActivity"
            android:exported="true" />
        <activity
            android:name=".activitys.editimage.EditImageActivity"
            android:exported="true" />
        <activity
            android:name=".activitys.main.MainActivity"
            android:exported="true"
            android:theme="@style/FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_path" />
        </provider>
    </application>

</manifest>